#include <stdio.h>
#include <stdlib.h>

int main()
{
        int a,q,w;

        scanf("%d", &a);
        q = a*a;
        w = a*a*a;
        printf("%d\n", q);
        printf("%d", w);
        return 0;
}
