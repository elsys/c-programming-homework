#include <stdio.h>
#include <stdlib.h>

int main()
{
int n,m,k,a,sum=0,i=0;
scanf("%d", &n);
scanf("%d", &m);
scanf("%d", &k);
a=2*m/100;
while(n>i)
{sum=sum+m-(i/k*a);
 i++;
}
printf("\n %d", sum);
return 0;
}
